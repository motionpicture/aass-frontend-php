<?php require __DIR__ . '/../header.phtml' ?>

<script src="/js/media/edit.js"></script>

<h1 class="page-header"><?= ($form->get('id')->getValue()) ? "{$form->get('title')->getValue()}の動画編集" : '動画追加' ?></h1>

<div class="progress" style="display:none;">
    <p id="progressText"></p>
</div>

<p class="error" style="color: #FF0000;"></p>

<form enctype="multipart/form-data" method="POST">
    <?= $form->render('id') ?>
    <div class="form-group">
        <?= $form->render('title', ['class' => 'form-control']) ?>
        <?= $form->render('description', ['class' => 'form-control']) ?>
        <?= $form->render('uploaded_by', ['class' => 'form-control']) ?>
        <?php if (!$form->get('id')->getValue()) { ?>
        <?= $form->render('file', ['class' => 'form-control']) ?>
        <?php } ?>
        <button type="button" class="btn btn-primary"><?= $form->getValue('id') ?>登録</button>
    </div>
</form>

<?php require __DIR__ . '/../footer.phtml' ?>